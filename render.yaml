services:
  - type: web
    name: marketingtienda
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: PYTHON_ENV
        value: production
