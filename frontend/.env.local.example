# La unica variable del frontend. OJO: se incrusta al CONSTRUIR (npm run build),
# no se lee al arrancar. Cambiar la URL del backend obliga a reconstruir.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
